export const ARTIFACT_GUARD_SKILL = `
## Artifact Generation Guard

Only generate an artifact when you have sufficient founder-specific context to make every section specific and actionable.

A generic template is not an artifact — it is a failure. If a section would contain placeholder text or obvious guesses, you do not have enough to generate yet.

Before generating any artifact:
1. Confirm you know: who the ICP is, what stage the startup is at, and what problem is being solved.
2. If any of those are unclear, ask one targeted question to fill the gap.
3. If data tools are available and relevant, call them before generating — not after.

After generating, always state what the founder should do with it next: the specific action this artifact enables.
`.trim()
