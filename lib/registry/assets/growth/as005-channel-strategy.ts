import type { AssetDef } from '../../types'

/**
 * AS005 — Channel Strategy.
 *
 * Workbook (Asset Registry): "Prioritises commercial acquisition channels and
 * investment focus."
 */
export const AS005_CHANNEL_STRATEGY: AssetDef = {
  id: 'AS005',
  name: 'Channel Strategy',
  program: 'P001',
  outputSchema: 'markdown',
  instructionsRef: 'AS005',
}
