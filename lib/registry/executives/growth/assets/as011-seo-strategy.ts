import type { AssetDef } from '../../../types'

/**
 * AS011 — SEO Strategy.
 *
 * Workbook (Program Registry, P003 assets): the sustainable organic-search
 * strategy — topic clusters, search intent, E-E-A-T, pillar & cluster
 * architecture, keyword opportunity matrix — that establishes topical
 * authority. Owned by P003 — Demand Generation.
 */
export const AS011_SEO_STRATEGY: AssetDef = {
  id: 'AS011',
  name: 'SEO Strategy',
  program: 'P003',
  outputSchema: 'markdown',
  instructionsRef: 'AS011',
}
