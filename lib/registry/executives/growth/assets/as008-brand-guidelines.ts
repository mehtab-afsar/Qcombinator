import type { AssetDef } from '../../../types'

/**
 * AS008 — Brand Guidelines.
 *
 * Workbook (Asset Registry): "Defines visual identity, tone of voice and
 * communication standards." Owned by P002 — Brand Strategy.
 */
export const AS008_BRAND_GUIDELINES: AssetDef = {
  id: 'AS008',
  name: 'Brand Guidelines',
  program: 'P002',
  outputSchema: 'markdown',
  instructionsRef: 'AS008',
}
