import type { AssetDef } from '../../../types'

/**
 * AS007 — Brand Identity.
 *
 * Workbook (Asset Registry): "Defines brand identity, mission, vision, values
 * and positioning." Owned by P002 — Brand Strategy.
 */
export const AS007_BRAND_IDENTITY: AssetDef = {
  id: 'AS007',
  name: 'Brand Identity',
  program: 'P002',
  outputSchema: 'markdown',
  instructionsRef: 'AS007',
}
