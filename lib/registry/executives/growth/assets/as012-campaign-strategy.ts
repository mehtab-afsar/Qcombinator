import type { AssetDef } from '../../../types'

/**
 * AS012 — Campaign Strategy.
 *
 * Workbook (Program Registry, P003 assets): the repeatable campaign system —
 * AARRR/RACE funnels, lead magnets, landing pages, paid media, attribution —
 * that turns acquisition into a measurable, repeatable playbook. Owned by
 * P003 — Demand Generation.
 */
export const AS012_CAMPAIGN_STRATEGY: AssetDef = {
  id: 'AS012',
  name: 'Campaign Strategy',
  program: 'P003',
  outputSchema: 'markdown',
  instructionsRef: 'AS012',
}
